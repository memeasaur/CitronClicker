>implement recorded mouse movements instead of that terrible thing it has
>change the citron name on the taskbar to my current cps setting

>make cps value over 20 be a different color?
>make the double clicker toggleable with keybinds
    >ensure it doesn't ever run at the same time as the autoclicker
    >NVM this shit flags
>it might take 10ms to enable? I don't want that
>make that one setting not stop inventory clicks so I can use it (GL, this thing is only checking if the cursor is visible)
>make it only keybind toggle if minecraft focused?
>make it accept ";" and "'"